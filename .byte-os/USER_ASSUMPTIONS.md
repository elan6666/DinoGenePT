# User Assumptions

- Primary user can run Python CLIs and interpret ROC-AUC.
- Primary job: determine whether changing only the embedding backbone improves
  GenePT's gene-level biological signal.
- The user values real artifacts, tests, checksums, and server receipts over a
  narrative completion claim.
- The user will privately rotate and provision the Agent Plan credential.
- No real external user feedback has been supplied; all UX assumptions are
  internal workflow assumptions.

