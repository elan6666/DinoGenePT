training:
  phase: pretrain
  epochs: 6
  optimizer: adam
  learning_rate: 0.0001
  weight_decay: 0.0
  lr_gamma: 0.9
  selection_metric: none
  losses:
    prediction_weight: 0.0

evaluation:
  split: validation
  run: false

identity:
  experiment_id: scgpt-aligned-pretrain
